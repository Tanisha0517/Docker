version: '3.8'

services:
  tomcat:
    image: portfolio:latest      # Specify the image name
    build: .                  # Build the image from the Dockerfile in the current directory
    container_name: portfolio-container
    ports:
      - "8081:8080"           # Maps port 8080 on host to port 8080 in the container